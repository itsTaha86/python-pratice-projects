# Gallery

Very simple gallery for switching between photos of a specific directory.
Some sample photos are placed on Kivy Gallery to test the application.

To use this application install requirements:
```bash
pip install -r requirements.txt